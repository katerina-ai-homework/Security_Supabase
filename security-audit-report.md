# Отчёт по безопасности проекта Security_Supabase

**Дата анализа:** 2026-02-18  
**Инструмент:** Ручной статический анализ (Semgrep недоступен в системе)  
**Анализируемые файлы:** lib/gemini.ts, lib/supadata.ts, app/api/summarize/route.ts, app/page.tsx, components/home-input.tsx

---

## 🔴 КРИТИЧЕСКИЕ УЯЗВИМОСТИ

### 1. Отсутствие Rate Limiting на API Endpoint
**Файл:** [`app/api/summarize/route.ts`](app/api/summarize/route.ts:51)  
**CWE:** CWE-770 (Allocation of Resources Without Limits or Throttling)  
**OWASP:** API4:2023 - Unrestricted Resource Consumption

**Описание:**  
API endpoint `/api/summarize` не имеет ограничений на количество запросов. Злоумышленник может отправить множество запросов, что приведёт к:
- Исчерпанию API квоты Google Gemini
- Исчерпанию API квоты Supadata
- DoS атаке на сервер

**Рекомендация:**
```typescript
// Установить rate limiting middleware
import { Ratelimit } from "@unkey/ratelimit";
// или использовать Vercel Edge Config для лимитов
```

---

### 2. Информационная утечка в сообщениях об ошибках
**Файл:** [`app/api/summarize/route.ts`](app/api/summarize/route.ts:100-116)  
**CWE:** CWE-209 (Generation of Error Message Containing Sensitive Information)  
**OWASP:** API3:2023 - Broken Object Property Level Authorization

**Описание:**
```typescript
console.error('Error in /api/summarize:', error);
// ...
const errorMessage = error instanceof Error 
  ? error.message  // Потенциальная утечка внутренней информации
  : 'An unknown error occurred';
```

Детали внутренних ошибок (включая API ключи, пути к файлам, структуру БД) могут быть раскрыты клиенту.

**Рекомендация:**
```typescript
// В продакшене возвращать обобщённые сообщения
const errorMessage = process.env.NODE_ENV === 'production'
  ? 'Произошла ошибка при обработке запроса'
  : error instanceof Error ? error.message : 'Unknown error';
```

---

## 🟠 ВЫСОКИЙ УРОВЕНЬ РИСКА

### 3. Отсутствие валидации размера входных данных
**Файл:** [`app/api/summarize/route.ts`](app/api/summarize/route.ts:54-55)  
**CWE:** CWE-20 (Improper Input Validation)

**Описание:**
```typescript
const body: SummarizeRequest = await request.json();
const { url } = body;
```

Нет проверки на максимальную длину URL. Злоумышленник может отправить URL размером в несколько мегабайт.

**Рекомендация:**
```typescript
const MAX_URL_LENGTH = 2048; // Стандартный лимит для URL
if (url.length > MAX_URL_LENGTH) {
  return NextResponse.json(
    { success: false, error: 'URL is too long' },
    { status: 400 }
  );
}
```

---

### 4. Отсутствие CSRF защиты
**Файл:** [`app/api/summarize/route.ts`](app/api/summarize/route.ts:51)  
**CWE:** CWE-352 (Cross-Site Request Forgery)

**Описание:**  
POST запросы не защищены от CSRF атак. Злоумышленник может создать вредоносную страницу, которая отправляет запросы от имени авторизованного пользователя.

**Рекомендация:**
- Использовать SameSite cookies
- Добавить CSRF токены
- Проверять Origin/Referer заголовки

---

### 5. Небезопасная конфигурация TypeScript
**Файл:** [`next.config.mjs`](next.config.mjs:3-5)  
**CWE:** CWE-489 (Active Debug Code)

**Описание:**
```javascript
typescript: {
  ignoreBuildErrors: true,
},
```

Это позволяет деплоить код с ошибками типизации, что может привести к runtime ошибкам и неожиданному поведению.

**Рекомендация:**  
Удалить `ignoreBuildErrors` и исправить все ошибки типизации.

---

## 🟡 СРЕДНИЙ УРОВЕНЬ РИСКА

### 6. Отсутствие Content Security Policy (CSP)
**Файл:** [`next.config.mjs`](next.config.mjs:1-11)  
**CWE:** CWE-1021 (Improper Restriction of Rendered UI Layers)

**Описание:**  
Не настроены заголовки безопасности CSP, что увеличивает риск XSS атак.

**Рекомендация:**
```javascript
const nextConfig = {
  async headers() {
    return [{
      source: '/:path*',
      headers: [
        { key: 'X-Content-Type-Options', value: 'nosniff' },
        { key: 'X-Frame-Options', value: 'DENY' },
        { key: 'X-XSS-Protection', value: '1; mode=block' },
        { key: 'Content-Security-Policy', value: "default-src 'self'; ..." },
      ],
    }];
  },
}
```

---

### 7. Потенциальная инъекция через промпт AI
**Файл:** [`lib/gemini.ts`](lib/gemini.ts:140-165)  
**CWE:** CWE-74 (Improper Neutralization of Special Elements)

**Описание:**
```typescript
function createSummaryPrompt(transcript: string): string {
  return `...
Транскрипт:
${transcript}`;
}
```

Данные от пользователя (transcript) напрямую вставляются в промпт без санитизации. Это может привести к:
- Prompt injection атакам
- Утечке системного промпта
- Манипуляции выводом AI

**Рекомендация:**
- Добавить валидацию и санитизацию transcript
- Ограничить максимальную длину
- Использовать escape-последовательности для пользовательского контента

---

### 8. Отсутствие таймаута для AI запросов
**Файл:** [`lib/gemini.ts`](lib/gemini.ts:183-210)  
**CWE:** CWE-400 (Uncontrolled Resource Consumption)

**Описание:**  
Запросы к Gemini API не имеют явного таймаута, что может привести к зависанию запросов.

**Рекомендация:**
```typescript
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 60000);
// Использовать controller.signal в запросе
```

---

## 🟢 НИЗКИЙ УРОВЕНЬ РИСКА

### 9. Дублирование кода валидации URL
**Файлы:** [`lib/supadata.ts`](lib/supadata.ts:146-148), [`components/home-input.tsx`](components/home-input.tsx:13-17)

**Описание:**  
Функция `isValidYoutubeUrl` определена в двух местах с немного разной логикой. Это может привести к несогласованности валидации.

**Рекомендация:**  
Вынести в общий модуль `lib/validation.ts`.

---

### 10. Отсутствие логирования безопасности
**Файл:** [`app/api/summarize/route.ts`](app/api/summarize/route.ts:51-118)

**Описание:**  
Нет структурированного логирования для мониторинга подозрительной активности.

**Рекомендация:**
```typescript
// Добавить логирование с уровнем безопасности
logger.info('API request', { 
  ip: request.headers.get('x-forwarded-for'),
  url: url.substring(0, 50), // Обрезать для логов
  timestamp: new Date().toISOString()
});
```

---

## ✅ ПОЛОЖИТЕЛЬНЫЕ АСПЕКТЫ

1. **API ключи в переменных окружения** - [`lib/gemini.ts:172`](lib/gemini.ts:172), [`lib/supadata.ts:50`](lib/supadata.ts:50)
2. **Проверка наличия API ключей** - [`lib/gemini.ts:174-176`](lib/gemini.ts:174-176)
3. **Валидация YouTube URL** - [`app/api/summarize/route.ts:66-71`](app/api/summarize/route.ts:66-71)
4. **Таймаут для HTTP запросов** - [`lib/supadata.ts:24`](lib/supadata.ts:24) (30 секунд)
5. **Ранний возврат при ошибках** - используется паттерн early-exit

---

## 📊 ИТОГОВАЯ ОЦЕНКА

| Категория | Количество |
|-----------|------------|
| 🔴 Критические | 2 |
| 🟠 Высокие | 3 |
| 🟡 Средние | 3 |
| 🟢 Низкие | 2 |

**Общая оценка:** Проект имеет несколько серьёзных уязвимостей, требующих внимания перед продакшеном.

---

## 🔧 РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТУ

1. **Немедленно:** Добавить Rate Limiting на API endpoints
2. **Немедленно:** Убрать `ignoreBuildErrors` из next.config.mjs
3. **Высокий приоритет:** Добавить валидацию размера входных данных
4. **Высокий приоритет:** Настроить заголовки безопасности (CSP, X-Frame-Options)
5. **Средний приоритет:** Добавить санитизацию промптов для AI
6. **Средний приоритет:** Реализовать структурированное логирование

---

*Отчёт сгенерирован на основе ручного статического анализа кода. Для более глубокого анализа рекомендуется установить Semgrep и выполнить:*
```bash
semgrep --config p/security-audit --config p/owasp-top-ten .
```
