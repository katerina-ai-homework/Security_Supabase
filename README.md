# YouTube Summarizer

Сервис для генерации кратких саммари YouTube-видео с использованием AI.

## 🚀 Возможности

- **Автоматическая генерация саммари** для любого YouTube видео
- **TL;DR** — краткое описание видео в одном предложении
- **Структурированные тезисы** — список основных идей
- **Поддержка разных языков** — саммари на русском языке
- **Минималистичный дизайн** — фокус на контенте

## 🛠️ Технологии

### Frontend
- **Next.js 16** — React фреймворк с App Router
- **TypeScript** — типизация
- **Tailwind CSS** — стилизация
- **shadcn/ui** — UI компоненты

### Backend
- **Next.js API Routes** — серверless функции
- **Supadata API** — получение транскриптов видео
- **Google Gemini AI** — генерация саммари (модель `gemini-flash-lite-latest`)
- **Axios** — HTTP клиент

### Testing
- **Jest** — тестовый фреймворк
- **Полный мокинг** внешних API

### Deployment
- **Vercel** — хостинг (бесплатный аккаунт)
- **GitHub** — CI/CD

## 📋 Требования

- Node.js 18+
- npm, pnpm или yarn
- Аккаунты на:
  - [Supadata](https://supadata.ai/)
  - [Google AI Studio](https://makersuite.google.com/app/apikey)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd Youtube_Summary
```

### 2. Установка зависимостей

```bash
pnpm install
# или
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
# Supadata API Key
SUPADATA_API_KEY=your_supadata_api_key_here

# Google Gemini API Key
GOOGLE_API_KEY=your_google_api_key_here
```

Получите ключи:
- **Supadata:** https://supadata.ai/
- **Google Gemini:** https://makersuite.google.com/app/apikey

### 4. Запуск dev сервера

```bash
pnpm dev
# или
npm run dev
```

Откройте http://localhost:3000 в браузере.

## 🧪 Тестирование

### Запуск тестов

```bash
pnpm test
# или
npm test
```

### Запуск тестов в watch режиме

```bash
pnpm test:watch
# или
npm run test:watch
```

## 📦 Деплой на Vercel

### Через Vercel Dashboard

1. **Подключите репозиторий GitHub** к Vercel
2. **Настройте переменные окружения:**
   - Перейдите в Settings → Environment Variables
   - Добавьте `SUPADATA_API_KEY`
   - Добавьте `GOOGLE_API_KEY`
3. **Задеплойте проект**

Подробная инструкция: [`docs/vercel-setup.md`](docs/vercel-setup.md)

### Через Vercel CLI

```bash
# Установка Vercel CLI
npm i -g vercel

# Вход в Vercel
vercel login

# Деплой
vercel

# Добавление переменных окружения
vercel env add SUPADATA_API_KEY
vercel env add GOOGLE_API_KEY
```

## 📁 Структура проекта

```
Youtube_Summary/
├── app/
│   ├── api/
│   │   └── summarize/
│   │       └── route.ts          # API endpoint для суммирования
│   ├── layout.tsx                # Корневой layout
│   ├── page.tsx                  # Главная страница
│   └── globals.css               # Глобальные стили
├── components/
│   ├── home-input.tsx            # Компонент ввода URL
│   ├── loading-state.tsx         # Компонент загрузки
│   ├── summary-result.tsx        # Компонент результата
│   └── ui/                       # shadcn/ui компоненты
├── lib/
│   ├── supadata.ts               # Интеграция с Supadata API
│   ├── gemini.ts                 # Интеграция с Gemini API
│   └── utils.ts                  # Утилиты
├── tests/
│   └── backend.test.ts           # Юнит-тесты бэк-энда
├── docs/
│   └── vercel-setup.md           # Документация по Vercel
├── plans/
│   └── backend-architecture.md   # План архитектуры
├── .env.local                    # Локальные переменные (не в git)
├── .gitignore                    # Исключения git
├── jest.config.js                # Конфигурация Jest
├── jest.setup.js                 # Настройка Jest
├── next.config.mjs               # Конфигурация Next.js
├── package.json                  # Зависимости
├── tsconfig.json                 # Конфигурация TypeScript
└── README.md                     # Этот файл
```

## 🔧 API

### POST /api/summarize

Генерирует саммари для YouTube видео.

**Request:**
```json
{
  "url": "https://www.youtube.com/watch?v=VIDEO_ID"
}
```

**Response (Success):**
```json
{
  "success": true,
  "data": {
    "videoTitle": "Название видео",
    "channelName": "Название канала",
    "thumbnailUrl": "https://img.youtube.com/vi/VIDEO_ID/maxresdefault.jpg",
    "sections": [
      {
        "title": "TL;DR",
        "points": ["Краткое описание видео"]
      },
      {
        "title": "Основные тезисы",
        "points": ["Тезис 1", "Тезис 2", ...]
      }
    ]
  }
}
```

**Response (Error):**
```json
{
  "success": false,
  "error": "Описание ошибки"
}
```

### GET /api/summarize

Проверяет доступность API.

**Response:**
```json
{
  "success": true,
  "message": "YouTube Summarizer API is available",
  "version": "1.0.0"
}
```

## 🎨 Vercel Best Practices

Проект следует рекомендациям Vercel для оптимизации производительности:

- **async-parallel** — параллельное выполнение независимых операций
- **server-cache** — кэширование результатов для дедупликации
- **server-serialization** — минимизация данных для клиента
- **js-early-exit** — ранний возврат при ошибках
- **bundle-size** — оптимизация размера бандла

Подробнее: [`plans/backend-architecture.md`](plans/backend-architecture.md)

## 🔒 Безопасность

- API ключи хранятся в переменных окружения
- `.env.local` исключён из git
- Валидация URL на клиенте и сервере
- Обработка ошибок без утечки информации

## 📝 Лицензия

MIT

## 🤝 Вклад

Contributions are welcome!

## 📞 Поддержка

Если у вас есть вопросы или проблемы, создайте issue в репозитории.
